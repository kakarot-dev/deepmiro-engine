name: Release

on:
  push:
    tags: ["v*"]

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      engine: ${{ steps.filter.outputs.engine }}
      mcp: ${{ steps.filter.outputs.mcp }}
      twhin: ${{ steps.filter.outputs.twhin }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2
      - id: filter
        run: |
          CHANGED=$(git diff --name-only HEAD~1 HEAD 2>/dev/null || echo "")
          echo "engine=false" >> "$GITHUB_OUTPUT"
          echo "mcp=false" >> "$GITHUB_OUTPUT"
          echo "twhin=false" >> "$GITHUB_OUTPUT"
          if echo "$CHANGED" | grep -qE '^(engine/|docker/Dockerfile\.backend)'; then
            echo "engine=true" >> "$GITHUB_OUTPUT"
          fi
          if echo "$CHANGED" | grep -qE '^(mcp-server/|docker/Dockerfile$)'; then
            echo "mcp=true" >> "$GITHUB_OUTPUT"
          fi
          if echo "$CHANGED" | grep -qE '^(twhin-sidecar/|docker/Dockerfile\.twhin-sidecar)'; then
            echo "twhin=true" >> "$GITHUB_OUTPUT"
          fi
          # If nothing matched (e.g. shared files, helm, etc.), rebuild all three
          if ! echo "$CHANGED" | grep -qE '^(engine/|mcp-server/|twhin-sidecar/|docker/)'; then
            echo "engine=true" >> "$GITHUB_OUTPUT"
            echo "mcp=true" >> "$GITHUB_OUTPUT"
            echo "twhin=true" >> "$GITHUB_OUTPUT"
          fi

  build-and-push:
    needs: changes
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        include:
          - name: engine
            file: docker/Dockerfile.backend
            image: deepmiro-backend
            changed: ${{ needs.changes.outputs.engine }}
          - name: mcp
            file: docker/Dockerfile
            image: deepmiro-mcp
            changed: ${{ needs.changes.outputs.mcp }}
          - name: twhin-sidecar
            file: docker/Dockerfile.twhin-sidecar
            image: deepmiro-twhin-sidecar
            changed: ${{ needs.changes.outputs.twhin }}
    steps:
      - if: matrix.changed == 'true'
        uses: actions/checkout@v4
      - if: matrix.changed == 'true'
        uses: docker/setup-buildx-action@v3
      - if: matrix.changed == 'true'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - if: matrix.changed == 'true'
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.file }}
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/${{ matrix.image }}:latest
            ghcr.io/${{ github.repository_owner }}/${{ matrix.image }}:${{ github.ref_name }}

  desktop-extension:
    needs: changes
    if: needs.changes.outputs.mcp == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Build MCP server + pack .mcpb
        working-directory: mcp-server
        run: |
          npm ci
          npm run build
          npm install -g @anthropic-ai/mcpb
          cp ../static/image/deepmiro-lockup.png icon.png
          mcpb pack
          mv *.mcpb deepmiro.mcpb
      - name: Upload to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: mcp-server/deepmiro.mcpb
          tag_name: ${{ github.ref_name }}

  publish-mcp-registry:
    needs: changes
    if: needs.changes.outputs.mcp == 'true'
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Install mcp-publisher
        run: |
          curl -L "https://github.com/modelcontextprotocol/registry/releases/latest/download/mcp-publisher_linux_amd64.tar.gz" \
            | tar xz mcp-publisher
          sudo mv mcp-publisher /usr/local/bin/
      - name: Login via GitHub OIDC
        run: mcp-publisher login github-oidc
      - name: Publish to MCP registry
        run: mcp-publisher publish
